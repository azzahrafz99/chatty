<%= form_with(model: @room, remote: true, class: 'd-flex room-form') do |f| %>
  <%= f.text_field :name, class: 'form-control', autocomplete: 'off' %>
  <%= f.submit data: { disable_with: false }, class: 'btn btn-primary' %>
<% end %>
