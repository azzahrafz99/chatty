<div class="user">
  <div class="avatar">
    <% if user.avatar.attached? %>
      <%= image_tag user.avatar %>
    <% else %>
      <%= image_tag 'avatar.svg' %>
    <% end %>
  </div>

  <%= link_to user.email, direct_message_path(user) %>
</div>
